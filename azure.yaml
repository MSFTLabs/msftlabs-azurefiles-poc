# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
# ---------------------------------------------------------------------------
# azure.yaml - Azure Developer CLI (azd) project configuration
#
# Run "azd up" to deploy. AZD will:
#   1. Prompt for environment name and Azure location
#   2. Run the Bicep deployment (infra/main.bicep with infra/main.bicepparam)
#   3. Print outputs (resource names, endpoints, etc.)
#
# Ref: https://learn.microsoft.com/azure/developer/azure-developer-cli/
# ---------------------------------------------------------------------------

name: azfiles-poc

infra:
  provider: bicep
  path: infra
  module: main
